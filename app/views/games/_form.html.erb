<%= form_for(@game) do |f| %>
  <% if @game.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@game.errors.count, "error") %> prohibited this game from being saved:</h2>

      <ul>
      <% @game.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

   <div class="field">
    <%= f.label "Play against:"%><br />
    <%= f.select :player1, options_from_collection_for_select(User.all, "id", "name")%>
  </div>

 <div class="field">

 <%= f.label "Enter dimension:"%><br />
 <%= f.text_field :dimension%>
 </div>
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
